1.
getElementById returns one element by id
getElementsByClassName returns multiple elements by class
 querySelector returns the first matching CSS selector
 and querySelectorAll returns all matching elements

2. first we create element by document.createElement() then use appendChild()
3. Event bubbling is when an event on a child element automatically moves up to its parents.
4. 
5. preventDefault() stops the browser’s default action and stopPropagation() stops the event from parent elements.
